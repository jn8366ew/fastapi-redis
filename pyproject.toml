[project]
name = "fastapi-redis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "redis>=8.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.6",
]
